#!/usr/bin/env python

import asyncio
import logging

from reeven.van.astro.pmc import run_socket_server

logging.basicConfig(
    format="%(asctime)s:%(levelname)s:%(name)s:%(message)s",
    level=logging.INFO,
)

asyncio.run(run_socket_server())
